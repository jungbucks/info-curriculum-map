# 정보과 교육과정 학습 맵 — 연구 분석 도구 (진입점)

> 2022 개정 교육과정 정보과 계열(초 실과 → 중 정보 → 고 정보 → 진로·융합선택)의 학습 위계를
> 시각화하고 **계열성(지식 확장)·결손 지점을 대조·판정·기록**하는 **연구용** 도구. 교사용 참고 사이트 아님.
> **판정 근거를 남기는 것이 화면을 예쁘게 만드는 것보다 우선.**

## 스택 (고정 — 바꾸지 말 것)
- 순수 HTML/CSS/JS. **빌드·번들러·프레임워크 없음.** `<script type="module">`만.
- GitHub Pages에 그대로 배포. **외부 라이브러리 금지**(D3·Cytoscape 포함).
- 상태 = localStorage + JSON export/import. 백엔드·인증 없음.
- 배포: **github.com/jungbucks/info-curriculum-map (main)**. Pages 활성화는 사용자 몫.

## 데이터 (`data/`)
| 파일 | 성격 |
|---|---|
| `seed.json` | 원본 추출본(실데이터). **읽기 전용·수정 금지.** 파생물을 seed에 되쓰지 말 것 |
| `lanes.json` | 영역 → 레인(행) 배정. 수동·수정가능. **투트랙**(integrated/separated). 키=`<과목명>#<area_no>` |
| `edges.json` | 노드(셀) 간 간선 (수동 작성). kind: hierarchy/extension/transition |
| `gaps.json` | 결손 판정 결과 (화면3 자동 생성·재계산). **논문 방법론 절 원자료** |

**규모(실데이터)**: 과목 6 · 영역 24 · 핵심 아이디어 53 · 내용 요소 210(지식71/과정80/가치59) · 성취기준 110. ⚠️내용요소 범주 구분점은 `⋅`(U+22C5) — 코드는 `util.elemsByCat`가 접두어 매칭으로 흡수.

**레인 모델(투트랙, 사용자 확정)**: 5 백본 레인 = 컴퓨팅 시스템·데이터·알고리즘과 프로그래밍·인공지능·디지털 문화.
- `integrated`: 진로·융합(인기초·데과·소생)을 5 백본에 녹임(소생 5영역은 의미별 분산). 초등 단일 영역은 4레인에 시드(◐).
- `separated`: 인기초·데과·소생을 독립 레인으로 → 백본 L4 공백 = by_design 결손이 드러남.
- `store.activeTrack()`가 활성 트랙 반환. 트랙 전환(격자 화면 토글) 시 대조·결손 재계산.
- ⚠️ 초등#5는 4레인 다중배정이라 L1에서 같은 성취기준이 복제 카운트됨(의도적 시드) — 레인별 분할 여지.

## 파일 → 역할 매핑
| 파일 | 역할 |
|---|---|
| `index.html` | 3화면 셸(탭) + export/import 푸터 |
| `js/main.js` | 진입점: 로드 → 탭 라우팅(URL 해시) → 화면 렌더 · export/import·reset 배선 |
| `js/store.js` | 데이터 로드·상태·localStorage 영속화·JSON export/import (seed 읽기전용) · activeTrack/areaKey |
| `js/util.js` | `$`/`esc`/`downloadJson`/`setStatus` · `elemsByCat`/`catKey` · `terms`(토큰화, 대조·결손 공유) |
| `js/grid.js` | 화면1 격자 맵 (CSS Grid, 좌표계산 금지) + 상세 패널 · `cellData` export |
| `js/edges-svg.js` | 간선 SVG 오버레이 (getBoundingClientRect 측정·ResizeObserver, 좌표 하드코딩 금지) |
| `js/contrast.js` | 화면2 계열 대조표(레인 선택 → 범주×단계, 새 용어 강조) |
| `js/gaps.js` | 화면3 결손 판정(by_design/naming_mismatch) + 근거 문자열 |

## 3화면 요지
1. **격자 맵**: 행=영역 레인, 열=학교급 1~4. 노드=셀 DOM, 위치=Grid. 간선은 SVG 1장으로 덮고 DOM 측정해 path. **간선 구분은 색 아니라 선 모양**(레인이 색 씀). 셀=성취기준 개수. 클릭→상세(핵심아이디어·내용요소 3범주·성취기준). URL 해시 `#cell=…`. 트랙 전환 토글.
2. **계열 대조**(구 'Bloom 태깅' — 사용자 정정): 레인 하나 선택 → **행=내용요소 3범주(지식·이해/과정·기능/가치·태도)·열=4단계**. 한 행을 왼→오로 읽으면 초등→진로로 **지식이 어떻게 확장되나** 대조. 직전 단계까지 없던 용어를 담은 요소 = **새 요소(초록 밑줄)**.
3. **결손 리포트**: 레인을 학교급 순으로 훑어 2종 판정 + **근거 문자열 필수**.
   - `by_design`: 레인에 그 학교급 편제 없음(빈 셀). 기본 필터 제외.
   - `naming_mismatch`: 인접 present 단계 내용요소 용어 중복률 < 임계값(UI 슬라이더). 근거에 공통어 기록.
   - ~~stagnant~~: 인지수준 축을 안 쓰기로 해 **제거**.
   - 근거 예: `"데이터 · 1→2단계: 용어 중복률 0.19 < 0.2; 공통어 [데이터, 표현, 의미]"`

## 비목표 (하지 말 것)
프레임워크·번들러·백엔드·인증 / 애니메이션·전환·다크모드 등 꾸밈 / force-directed(좌표 정해짐) /
교사용 기능(수업자료·평가·인쇄) / **seed.json 수정 또는 파생물 되쓰기** / 요청 안 한 화면·기능.

## 로컬 확인
`fetch('data/*.json')`는 `file://`에서 막힘 → **정적 서버로 열 것**(index.html 더블클릭하면 백지!). 예: `node <scratchpad>/serve.mjs <프로젝트경로> <포트>` → `localhost:포트`. 스크린샷은 `msedge --headless=new`(구형 --headless는 캡처 실패). GitHub Pages(HTTP)에선 정상.

## 진행 상태 — 스펙 전 기능 완성·푸시됨
- 화면1 격자맵 ✅ · 화면2 계열 대조표 ✅(구 Bloom 교체, cbcaeee) · 화면3 결손 리포트 ✅ · 간선 SVG ✅. 전부 push됨.
- 실사용: 로컬서버 → 화면2에서 레인별 지식 확장 대조 → 화면1 격자·간선 + 트랙 전환 비교 → 화면3 결손 → gaps.json 내보내 논문 원자료.
- 후속(선택): 간선 authoring UI(현재 edges.json 수동 편집+[파일에서 다시 로드]) · L3→L4 확장간선은 데이터·인공지능만 시드됨(나머지 연구자 판단) · 초등#5 레인별 성취기준 분할 · Pages 활성화.

## 열린 설계 결정 — 모두 확정
- 레인 정의 → 투트랙(lanes.json).
- naming 중복률 산식 → 내용요소 3범주 토큰화(조사·어미 접미 제거, 2글자↑ 내용어) → 겹침계수 |A∩B|/min, 임계값 UI, 근거에 공통어 기록. `util.terms` STOP/규칙 수정 가능.
- 노드 단위 → **셀(레인×단계)**. 개별 성취기준은 격자에 위치 노드가 없어 측정 불가.
- 화면2 성격 → **인지수준(Bloom) 태깅 폐기, 계열 대조표로 확정**(사용자 정정 2026-07-29).
