# 정보과 교육과정 학습 맵 — 연구 분석 도구 (진입점)

> 2022 개정 교육과정 정보과 계열(초 실과 → 중 정보 → 고 정보 → 진로·융합선택)의 학습 위계를
> 시각화하고 **계열성 결손 지점을 판정·기록**하는 **연구용** 도구. 교사용 참고 사이트 아님.
> **판정 근거를 남기는 것이 화면을 예쁘게 만드는 것보다 우선.**

## 스택 (고정 — 바꾸지 말 것)
- 순수 HTML/CSS/JS. **빌드·번들러·프레임워크 없음.** `<script type="module">`만.
- GitHub Pages에 그대로 배포. **외부 라이브러리 금지**(D3·Cytoscape 포함).
- 상태 = localStorage + JSON export/import. 백엔드·인증 없음.

## 데이터 (`data/`)
| 파일 | 성격 |
|---|---|
| `seed.json` | 원본 추출본. **읽기 전용·수정 금지.** 파생물을 seed에 되쓰지 말 것 |
| `lanes.json` | 영역 → 레인(행) 배정. 수동·수정가능. **투트랙**(integrated/separated). 키=`<과목명>#<area_no>` |
| `bloom.json` | 서술어 → 인지수준(1~6) 매핑 (화면2 수동 태깅 결과) |
| `edges.json` | 노드 간 간선 (수동 작성). kind: hierarchy/extension/transition |
| `gaps.json` | 결손 판정 결과 (화면3 자동 생성·재계산). **논문 방법론 절 원자료** |

**레인 모델(투트랙, 사용자 확정)**: 5 백본 레인 = 컴퓨팅 시스템·데이터·알고리즘과 프로그래밍·인공지능·디지털 문화.
- `integrated`: 진로·융합(인기초·데과·소생)을 5 백본에 녹임(소생 5영역은 의미별 분산). 초등 단일 영역은 4레인에 시드(◐).
- `separated`: 인기초·데과·소생을 독립 레인으로 → 백본 L4 공백 = by_design 결손이 드러남.
- `store.activeTrack()`가 활성 트랙 반환. 트랙 전환 시 격자·결손 재계산.
- ⚠️ 초등#5는 4레인 다중배정이라 L1에서 같은 성취기준이 복제 카운트됨(의도적 시드) — 필요 시 성취기준을 레인별로 분할하는 정교화 여지.

**규모**: 영역 24 · 핵심 아이디어 53 · 내용 요소 210 · 성취기준 110.
현재 `seed.json`은 **스켈레톤(빈 골격)** — 사용자 실제 추출본으로 교체 대기.

## 파일 → 역할 매핑
| 파일 | 역할 |
|---|---|
| `index.html` | 3화면 셸(탭) + export/import 푸터 |
| `js/main.js` | 진입점: 로드 → 탭 라우팅(URL 해시) → 화면 렌더 |
| `js/store.js` | 데이터 로드·상태·localStorage 영속화·JSON export/import (seed 읽기전용) |
| `js/util.js` | `$`/`esc`/`downloadJson`/`setStatus` |
| `js/grid.js` | 화면1 격자 맵 (CSS Grid, 좌표계산 금지) + 상세 패널 |
| `js/edges-svg.js` | 간선 SVG 오버레이 (getBoundingClientRect 측정·ResizeObserver, 좌표 하드코딩 금지) |
| `js/bloom.js` | 화면2 서술어 추출(정규식)·Bloom 태깅 |
| `js/gaps.js` | 화면3 결손 판정(by_design/stagnant/naming_mismatch) + 근거 문자열 |

## 3화면 요지
1. **격자 맵**: 행=영역 레인, 열=학교급 1~4. 노드=DOM, 위치=Grid. 간선은 SVG 1장으로 덮고 DOM 측정해 path. **구분은 색 아니라 선 모양**(레인이 색 이미 씀). 셀=성취기준 개수+인지수준 농도. 클릭→상세. URL 해시 `#node=…`/`#cell=…`.
2. **인지수준 태깅**: 210개 개별 태깅 금지 → 과정·기능 서술어를 `/([가-힣]+하기|[가-힣]+보기)$/`로 추출해 **고유 서술어에만** Bloom 1~6. 복합 서술어("~하고 ~하기")는 분리해 수동. → `bloom.json`.
3. **결손 리포트**: 레인을 학교급 순으로 훑어 3종 판정 + **근거 문자열 필수**. 임계값 UI 조정→즉시 재계산. `by_design`은 기본 필터 제외. → `gaps.json`.
   - 근거 예: `"중2→고3: Bloom max 4→4, 서술어 '탐구하기'→'탐색하기'"`

## 비목표 (하지 말 것)
프레임워크·번들러·백엔드·인증 / 애니메이션·전환·다크모드 등 꾸밈 / force-directed(좌표 정해짐) /
교사용 기능(수업자료·평가·인쇄) / **seed.json 수정 또는 파생물 되쓰기** / 요청 안 한 화면·기능.

## 빌드 순서 (권장)
seed.json 확보 → 화면1(격자+간선) → 화면2(Bloom, gaps가 이 결과에 의존) → 화면3(결손).

## 로컬 확인
`fetch('data/*.json')`는 `file://`에서 막힘 → **정적 서버로 열 것**(예: `npx serve` 또는 node http). GitHub Pages(HTTP)에선 정상.

## 진행 상태 — 스펙 전 기능 완성
- 화면1 격자맵 ✅(6b2126c) · 화면2 Bloom 태깅 ✅(9dca715) · 화면3 결손 리포트 ✅(fdcabf2) · 간선 SVG ✅(fb3c023).
- 실사용 흐름: 로컬서버 → 화면2에서 서술어 31개 Bloom 태깅 → 화면1 색농도·화면3 stagnant 살아남 → 트랙 전환 비교 → gaps.json 내보내 논문 원자료.
- 후속(선택): 간선 authoring UI(현재는 edges.json 수동 편집+[파일에서 다시 로드]), 초등#5 레인별 성취기준 분할, GitHub Pages 배포(레포 생성 필요).

## 열린 설계 결정 — 모두 확정
- 레인 정의 → 투트랙(lanes.json).
- naming 중복률 산식 → 내용요소 3범주 토큰화(조사·어미 접미 제거, 2글자↑ 내용어) → 겹침계수 |A∩B|/min, 임계값 UI, 근거에 공통어 기록. gaps.js STOP/규칙 수정 가능.
- 노드 단위 → **셀(레인×단계)**. 개별 성취기준은 격자에 위치 노드가 없어 측정 불가.
